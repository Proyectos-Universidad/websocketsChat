export const base_api_url = 'http://localhost:4000/';

export const api_routes = {
  users_base: 'users',
  users_authenticate: 'users/authenticate',
  users_register: 'users/register',
  users_exists_by_username: 'users/exists/'
};

export const routes = {
  login: 'login', 
  register: 'register',
  chat: 'chat'
  
};